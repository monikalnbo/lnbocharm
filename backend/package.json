{
  "name": "codeforge-backend",
  "version": "0.1.0",
  "private": true,
  "description": "CodeForge 远程构建服务器：REST + WS(统一信封协议) + pyworker 桥",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "express": "^4.19.0",
    "node-pty": "^1.1.0",
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=20"
  }
}
